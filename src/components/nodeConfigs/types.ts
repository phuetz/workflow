// Re-export node configuration types from the main types directory
export type {
  FieldConfig,
  NodeConfigDefinition,
  ValidationResult
} from '../../types/nodeConfig';

export { validators, commonFields } from '../../types/nodeConfig';

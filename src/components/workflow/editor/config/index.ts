/**
 * Editor Configuration
 * Export all editor configuration constants
 */

export {
  nodeTypesMap,
  n8nEdgeTypes,
  connectionLineStyle,
  defaultEdgeOptions,
  scaleConfig,
  edgeStyleMap,
  defaultMarkerEnd,
  gridConfig,
  categoryColors,
} from './editorConfig';

export type { ViewMode, ConnectionStyle } from './editorConfig';

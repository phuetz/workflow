// Global teardown for Playwright tests
// Keeps a placeholder to align with playwright.config.ts
export default async function globalTeardown() {
  // No global resources to clean up at the moment
}

